<view class="container">
  <!-- 头部 -->
  <view class="index-header"></view>

  <!-- 主体 -->
  <view class="index-content clearfix">
    <view class="menu-item" wx:for="{{menu}}" data-idx="{{item.idx}}" bindtap="bindViewTap">
      {{item.name}}
    </view>
  </view>
</view>